import * as React from 'react';

interface Props {
    
}

interface State {
    
}

class BlockHTML extends React.Component<Props, State> {

    public constructor(props: Props){
        super(props);
    }

    public render(){
        return (
            <h1>BlockHTML TODO</h1>
        );
    }

}

export default BlockHTML;